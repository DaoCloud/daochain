import template from './local.html';
import controller from './local.controller';
import './local.scss';

let localComponent = {
  restrict: 'E' ,
  bindings: {},
  template,
  controller
};

export default localComponent;
