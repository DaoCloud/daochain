// import * as Store from '../../helpers/store';
// import * as state from '../../app.state.js';

class AuthStoreService {

  /*@ngInject*/
  constructor() {

  }

  getToken() {
    // return state.actions.getAuthToken();
  }

  getUserId() {
    // return state.actions.getAuthId();
  }

  setToken(token) {
    // return state.actions.setAuthToken(token);
  }

  setUserId(id) {
    // return state.actions.setAuthId(id);
  }

  clean() {
    // Store.clear();
  }

}

export default AuthStoreService;
