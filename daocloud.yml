
version: 2.0

build:
    lite_image:
        compile:
            dockerfile_path: /webui/Dockerfile
            build_dir: /webui/
            cache: true

        extract:
            - /root/dist

        package:
              dockerfile_path: Dockerfile
              build_dir: /
              cache: true
