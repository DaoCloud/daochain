
version: 2.0

build:
    lite_image:
        compile:
            dockerfile_path: /website/Dockerfile
            build_dir: /website/
            cache: true

        extract:
            - /app/dist

        package:
              dockerfile_path: Dockerfile
              build_dir: /
              cache: true
